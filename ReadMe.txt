Project is created in Eclipse IDE using Maven, TestNG, Cucumber and the relevant dependencies

Use config.properites file in config folder under src/test/resources to change the browser. Config is set to choose either firefox or Google chrome browsers.

Reports are generated in target folder: cucumber-html-report and surefire-reports